// string (Texto), Number (Sem distinção de inteiros e quebrados), Undefined, null, boolean (booleanos, true ou false), Symbol
const nome = 'Lucas'; // String
const nome1 = "Rachid"; // String
const nome2 = `Lucas`; // String

const num1 = 10; // Number - Em outras linguagens, também tem como INT de Integral ou numero inteiro
const num2 = 10.50 // Number - Em outras linguagens, também tem como FLOAT, que seriam números quebrados

let nomeAluno; // undefined = Não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória

const boolean = true; //Tipo booleano, só existe duas possibilidades de valor, true e false.
const boolean2 = false; //Tipo booleano, só existe duas possibilidades de valor, true e false.

console.log(typeof nomeAluno); // Para verificar o tipo primitivo da variável